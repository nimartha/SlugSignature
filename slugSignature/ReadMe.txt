1- Download/install node.js LTS: https://nodejs.org/en/download/
2- Open a terminal and run the following commands to check if the latest versions are installed:
$ node –-version
$ npm --version
Note: current versions should be as followings: Node LTS Version: 16.13.2 - npm 8.1.2
3- Set up development environment as followings:
	a) Open a terminal and ensure you are in the right directory, where package.json file is -> cse115a/slugSignature
	b) Run the following command:
		$ npm install
	c) To run the application, execute the following command
		$ npm start
	d) To run test, execute the following:
		$ npm test
	e) to run eslint, execute the following:
		$ npm run lint
